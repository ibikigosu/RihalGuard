# Setup guide — Meeting Summary Agent

```bash
python3 run.py
python3 evals/run.py
```

To implement for real:

1. Replace mock tool bodies with integrations.
2. Keep tool names synchronized with `rihalguard.json`.
3. Add domain-specific evals.
4. Run root validation before sharing.
