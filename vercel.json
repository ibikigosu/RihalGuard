{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "uv sync --locked",
  "buildCommand": "uv run --locked python scripts/validate.py && node scripts/validate-risk-tiers.mjs"
}
