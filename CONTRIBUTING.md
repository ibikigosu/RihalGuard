# Contributing

Keep changes small and reviewable. Every blueprint must include `blueprint.json`, `rihalguard.json`, `tools.json`, `system-prompt.md`, `workflow.md`, `run.py`, and `evals/run.py`. Run `python3 scripts/validate.py` before submitting changes.
